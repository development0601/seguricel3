@model IEnumerable<Seguricel3.Models.UsuarioViewModel>

@{
    Layout = "~/Views/Shared/_LayoutInternal.cshtml";
}

<script src="~/Scripts/Seguricel-DataGrid.js"></script>

<div>
    @Html.ActionLink(Resources.EtiquetasResource.labelNewLink, "CreateUsuario", "Seguridad", htmlAttributes: new { @class = "btn btn-default place-right" })
</div>
<div style="margin-top:60px; padding-top: 20px; border-top: 1px solid #000; width: 95%;">
    <table id="dataTable" class="dataTable" cellspacing="0" style="margin: auto;  width:100%; font-size: 12pt;">
        <thead>
            <tr>
                <th style="vertical-align: middle; width: 40%;">
                    @Html.DisplayNameFor(m => m.Nombre)
                </th>
                <th style="text-align:center; width: 25%;">
                    @Html.DisplayNameFor(m => m.NivelUsuario)
                </th>
                <th style="text-align:center; width: 15%;">
                    @Html.DisplayNameFor(m => m.EstadoUsuario)
                </th>
                <th style="text-align:center; width: 10%;">
                    @Html.DisplayNameFor(m => m.FechaUltimaConexion)
                </th>
                <th style="width: 5%;"></th>
                <th style="width: 5%;"></th>
            </tr>
        </thead>
        <tfoot>
            <tr>
                <td colspan="6">&nbsp;</td>
            </tr>
        </tfoot>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="vertical-align: middle;">
                        @Html.DisplayFor(m => item.Nombre)
                    </td>
                    <td>
                        @Html.DisplayFor(m => item.NivelUsuario)
                    </td>
                    <td>
                        @Html.DisplayFor(m => item.EstadoUsuario)
                    </td>
                    <td>
                        @Html.DisplayFor(m => item.FechaUltimaConexion)
                    </td>
                    <td style="text-align: center;">
                        <a href="@Url.Action("EditUsuario", "Seguridad", routeValues: new { id = item.Id })"><span class="mif-pencil" title=@Resources.EtiquetasResource.labelEditLink></span></a>
                    </td>
                    <td style="text-align: center;">
                        <a href="@Url.Action("DeleteUsuario", "Seguridad", routeValues: new { id = item.Id })" onclick="return confirm('@item.Nombre @Resources.EtiquetasResource.DeleteConfirmMessage');"><span class="mif-bin" title=@Resources.EtiquetasResource.labelDeleteLink></span></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>



